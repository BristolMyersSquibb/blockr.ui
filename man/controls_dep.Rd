% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{controls_dep}
\alias{controls_dep}
\title{Shared block controls}
\usage{
controls_dep()
}
\value{
An \code{\link[htmltools:tagList]{htmltools::tagList()}} of \link[htmltools:htmlDependency]{htmltools::htmlDependency} objects,
in load order.
}
\description{
The JavaScript and CSS of the controls blockr blocks are built from:
\code{Blockr.Select} (single, multi and menu), the Enter button of a field that
commits on Enter, the required-empty cue, the checkbox, the segmented
control, the gear tray and the placement routine for floating panels
(\code{Blockr.place}), all on the \code{window.Blockr} namespace, together with the
rows, pills, labels and fields they draw.
}
\details{
The stylesheets read the design tokens without fallbacks, so the
dependency brings \code{\link[=theme_dep]{theme_dep()}} along. Attach it from a block's UI;
dependencies are de-duplicated by name, so any number of blocks can.

The dependency names are the ones blockr.dplyr used while these files
lived there (\code{blockr-select-js}, \code{blockr-select-css}, \code{blockr-blocks-css}),
so a page never loads two copies of Select.
}
\examples{
shiny::fluidPage(
  controls_dep(),
  shiny::div(id = "cols"),
  shiny::tags$script(
    "Blockr.Select.multi(document.getElementById('cols'),",
    "  { options: ['mpg', 'cyl', 'disp'], selected: ['mpg'] });"
  )
)

}
