% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-navigator.R
\name{panel_nav_model}
\alias{panel_nav_model}
\title{Shape the grouped navigator model from raw pieces.}
\usage{
panel_nav_model(
  entries,
  stacks = list(),
  ungrouped = character(),
  extensions = character(),
  visible = character(),
  views = list(),
  view_labels = character(),
  active = NULL
)
}
\arguments{
\item{entries}{Named-by-id list of per-entry metadata, each
\code{list(type=, title=, subtitle=, icon=, color=, category=, package=)}.}

\item{stacks}{Ordered list of \code{list(id=, name=, color=, block_ids=)}.}

\item{ungrouped}{Character vector of block ids in no stack.}

\item{extensions}{Character vector of extension ids.}

\item{visible}{Character vector of currently-visible object ids.}

\item{views}{Named (by view id) list of object-id vectors per view.}

\item{view_labels}{Named character vector: view id -> display label.}

\item{active}{Active view id.}
}
\value{
A list of groups for \code{\link[=panel_navigator_ui]{panel_navigator_ui()}}.
}
\description{
The dock-specific data adapter (in blockr.dock) gathers per-entry display
metadata plus a few live-dock facts and hands them here; this builds the
grouped model \code{panel_navigator_ui()} renders — grouping blocks by stack,
flagging current-view visibility, and tagging other views. Pure
blockr.core, so the shaping lives next to the UI.
}
