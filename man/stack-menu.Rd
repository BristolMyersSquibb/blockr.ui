% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stack-menu.R
\name{stack-menu}
\alias{stack-menu}
\alias{stack_menu_ui}
\alias{stack_menu_server}
\alias{stack_menu_dep}
\title{Stack menu}
\usage{
stack_menu_ui(id, board, target = NULL)

stack_menu_server(id, board = NULL, target = NULL)

stack_menu_dep()
}
\arguments{
\item{id}{Module id. As usual for Shiny modules, pass \code{NS(id)("...")}
from the parent at the \verb{*_ui()} call site and the bare id to
\verb{*_server()}.}

\item{board}{The current board state. Used to resolve the eligible
pool of board blocks and (in edit mode) to look the target stack
up. In \code{stack_menu_ui()} this is a plain board value; in
\code{stack_menu_server()} it may be a \strong{reactive} returning the board
(to opt into live sync + validation) or \code{NULL} (default; static
snapshot, no validation).}

\item{target}{Stack to edit. \code{NULL} (default) selects the \emph{create}
flow; a non-empty character scalar selects the \emph{edit} flow and
names the stack being edited. \code{stack_menu_server()} also accepts a
reactive. The parameter name mirrors \code{\link[=block_browser_ui]{block_browser_ui()}}.}
}
\value{
\itemize{
\item \code{stack_menu_ui()} returns an \link[htmltools:builder]{htmltools::tag} with
\code{\link[=stack_menu_dep]{stack_menu_dep()}} attached.
\item \code{stack_menu_server()} returns a \link[shiny:reactive]{shiny::reactive} of the committed
\link[blockr.core:new_stack]{blockr.core::stacks} object (one id-keyed \code{dock_stack}), firing
once per confirm. Requires the suggested \code{blockr.dock} package.
\item \code{stack_menu_dep()} returns an \link[htmltools:htmlDependency]{htmltools::htmlDependency}.
}
}
\description{
A multi-select card-list block picker for stacks. The stack menu
lives inside the sidebar primitive and replaces the legacy
selectize-style chooser used by \code{blockr.dock}'s add / edit stack
flows. Each card represents a board \strong{instance} (a block already on
the board); clicking a card toggles its membership in the new stack.
A small panel-level form below the cards carries the stack-level
parameters (name, color, id).
}
\details{
The stack menu is a Shiny module. \code{stack_menu_ui(id, board, target)}
renders the panel; \code{stack_menu_server(id, board, target)} returns a
reactive that fires once per confirm with a \link[blockr.core:new_stack]{blockr.core::stacks}
object whose single member is a \code{dock_stack} built via
\code{blockr.dock::new_dock_stack()}, carrying the chosen name and colour
and keyed by its id - the typed id in create mode, the edited stack's
id (\code{target}) in edit mode. The consumer adds it to the board as-is,
with no reshaping. Colour is a first-class \code{dock_stack} attribute, so
the menu requires the suggested \code{blockr.dock} package.

The server owns \strong{validation} of the committed spec: it checks the
stack id (create mode), name, and colour against the current board and
surfaces failures via \code{\link[shiny:showNotification]{shiny::showNotification()}}, so the committed
reactive only ever fires with a valid spec and the consumer needs no
validators of its own. When \code{board} is passed to \code{stack_menu_server()}
as a \strong{reactive}, the module also keeps an open menu in sync with the
board: on every board change it pushes a \code{menu:sync} diff to its own
binding that inserts newly-eligible cards, removes vanished ones, and
leaves scroll / selection / in-progress inputs untouched - no
re-render. Pass \code{board = NULL} (the default) for the static
snapshot behaviour.

The flow is selected by \code{target}: \code{NULL} (default) is the \emph{create}
flow; a character scalar names the stack being edited. In edit mode
the function pre-selects the stack's current blocks, augments the
eligible pool with them (so the user can deselect them), and
pre-fills the name / color fields. An unknown id raises a clean
error.

Internally the panel publishes the selection set through the
\code{blockr.ui.stackMenu} \code{Shiny.InputBinding} (the root element's \code{id}
is \code{NS(id)("commit")}); the published value carries an internal
\code{nonce} so repeat confirms re-fire as events. The panel-level
name / color / id fields are normal Shiny inputs the server reads
directly; \code{stack_menu_server()} strips the nonce and composes the
full spec.
}
\examples{
if (interactive()) {
  shiny::shinyApp(
    ui = shiny::fluidPage(
      sidebar_ui("panel", side = "right"),
      shiny::actionButton("open", "Open stack menu")
    ),
    server = function(input, output, session) {
      committed <- stack_menu_server("menu")
      shiny::observeEvent(input$open, {
        show_sidebar(
          "panel",
          title = "New stack",
          ui = stack_menu_ui(session$ns("menu"), NULL)
        )
      })
      shiny::observeEvent(committed(), print(committed()))
    }
  )
}

}
