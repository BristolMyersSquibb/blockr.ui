% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-preview-render.R
\docType{data}
\name{html_table_render}
\alias{html_table_render}
\alias{html_table_result}
\alias{html_table_display}
\title{Render a result as the HTML table preview}
\usage{
html_table_render(result, session, page_size = 5L)

html_table_result(result, block, session)

html_table_display
}
\arguments{
\item{result}{A data frame or lazy table (\code{tbl_lazy}).}

\item{session}{The Shiny session.}

\item{page_size}{Rows per page.}

\item{block}{The block object (used to read board options).}
}
\value{
A \code{\link[shiny:renderUI]{shiny::renderUI()}} object.
}
\description{
\code{html_table_render()} returns a \code{\link[shiny:renderUI]{shiny::renderUI()}} that shows \code{result}
as the paginated, sortable table preview. \code{html_table_result()} is the
block-flavored wrapper that reads the \code{page_size} board option first.
}
\details{
\code{html_table_display} is the \link[blockr.core:tabular-display]{blockr.core::tabular_display} that wires this
preview into data, parser and transform blocks. Opt in per app with
\code{options(blockr.tabular_display = blockr.ui::html_table_display)}; the
display declares and triggers on a single board option, \code{page_size}, which
the HTML table reads to paginate.

Sort and page state live in the browser and arrive via Shiny inputs whose
ids are derived from the output name (via \code{\link[shiny:getCurrentOutputInfo]{shiny::getCurrentOutputInfo()}}),
so several previews in one module session stay independent. Sorting and
pagination are resolved by \code{\link[=table_page]{table_page()}}: in-memory frames use a cached
order index, lazy dbplyr tables are counted and paged in the database.
}
\keyword{datasets}
