% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link-menu.R
\name{link-menu}
\alias{link-menu}
\alias{link_menu_ui}
\alias{link_menu_server}
\alias{link_menu_dep}
\alias{link_eligible_pools}
\title{Link menu}
\usage{
link_menu_ui(id, board, anchor)

link_menu_server(id)

link_menu_dep()

link_eligible_pools(board, anchor)
}
\arguments{
\item{id}{Module id. As usual for Shiny modules, pass \code{NS(id)("...")}
from the parent at the \verb{*_ui()} call site and the bare id to
\verb{*_server()}.}

\item{board}{The current board state.}

\item{anchor}{Block id the new link is anchored on (the
right-clicked block in the dock flow). A non-empty character
scalar; must be a member of \code{\link[blockr.core:board_blocks]{blockr.core::board_block_ids()}}.}
}
\value{
\itemize{
\item \code{link_menu_ui()} returns an \link[htmltools:builder]{htmltools::tag} with
\code{\link[=link_menu_dep]{link_menu_dep()}} attached.
\item \code{link_menu_server()} returns a \link[shiny:reactive]{shiny::reactive} of the committed
link spec (a list), firing once per commit.
\item \code{link_menu_dep()} returns an \link[htmltools:htmlDependency]{htmltools::htmlDependency}.
\item \code{link_eligible_pools()} returns
\verb{list(outgoing = <chr>, incoming = <chr>)} using the same
eligibility rules \code{link_menu_ui()} uses for its initial render.
}
}
\description{
A bidirectional card-list link picker. The link menu lives inside
the sidebar primitive and replaces the legacy selectize-style
chooser used by \code{blockr.dock}'s add-link flow. Each card represents
a board \strong{instance} that the new link can attach to from either
direction relative to a fixed \code{anchor} block (the right-clicked
block in the dock flow).
}
\details{
The link menu is a Shiny module. \code{link_menu_ui(id, board, anchor)}
renders the panel; \code{link_menu_server(id)} returns a reactive that
fires once per commit with the link spec:
\code{list(source, target, link_id, block_input)}. The JS binding
composes \code{source} / \code{target} from the clicked card's
\code{data-direction} and the panel's \code{data-anchor}, so the consumer
never has to know the orientation up front:
\itemize{
\item \code{data-direction = "outgoing"} -> \code{source = anchor}, \code{target = card}
\item \code{data-direction = "incoming"} -> \code{source = card},   \code{target = anchor}
}

The panel renders up to two category-grouped sections, labelled
from the anchor's port-role perspective:
\itemize{
\item \strong{OUTPUT TO} (anchor -> card): blocks the anchor can link to.
Eligible = every other board block that has at least one free
named input port or is variadic, excluding any that already
reach the anchor (would close a cycle).
\item \strong{INPUT FROM} (card -> anchor): blocks that can link into the
anchor. Only rendered when the anchor itself has at least one
free named input port or is variadic. Eligible = every other
board block, excluding any that the anchor already reaches.
}

When both pools are empty the panel renders an in-place empty-state
instead of returning \code{NULL}.

Internally the panel publishes its committed link spec through the
\code{blockr.ui.linkMenu} \code{Shiny.InputBinding} (the root element's \code{id}
is \code{NS(id)("commit")}). The binding's \code{receiveMessage} accepts a
\code{list(type = "pool-update", eligible = list(outgoing, incoming), link_id_seed)} payload so consumers can keep the menu open across
multiple commits and push live eligibility updates without
re-rendering - the multi-link session pattern. Use
\code{\link[=link_eligible_pools]{link_eligible_pools()}} to compute the post-commit pool on the
consumer side and send it via \code{session$sendInputMessage()} (see
\code{shiny::session}).
}
\examples{
if (interactive()) {
  shiny::shinyApp(
    ui = shiny::fluidPage(
      sidebar_ui("panel", side = "right"),
      shiny::actionButton("open", "Open link menu")
    ),
    server = function(input, output, session) {
      board <- blockr.core::new_board(
        blockr.core::as_blocks(list(
          a = blockr.core::new_dataset_block(),
          b = blockr.core::new_head_block()
        ))
      )
      committed <- link_menu_server("menu")
      shiny::observeEvent(input$open, {
        show_sidebar(
          "panel", title = "Connect a",
          ui = link_menu_ui(session$ns("menu"), board, anchor = "a")
        )
      })
      shiny::observeEvent(committed(), print(committed()))
    }
  )
}

}
