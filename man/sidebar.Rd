% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sidebar.R
\name{sidebar}
\alias{sidebar}
\alias{sidebar_ui}
\alias{sidebar_dep}
\alias{show_sidebar}
\alias{hide_sidebar}
\alias{sidebar_state}
\alias{keep_or_hide_sidebar}
\title{Sidebar primitive}
\usage{
sidebar_ui(
  id,
  side = c("right", "left"),
  width = "360px",
  mode = c("overlay", "push")
)

sidebar_dep()

show_sidebar(id, ui, title = NULL, session = shiny::getDefaultReactiveDomain())

hide_sidebar(id, session = shiny::getDefaultReactiveDomain())

sidebar_state(id, session = shiny::getDefaultReactiveDomain())

keep_or_hide_sidebar(
  id,
  ui,
  title = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{A character scalar. The DOM id used for the panel element. The
same id is what \code{show_sidebar()} / \code{hide_sidebar()} target and what R
reads as \code{input[[id]]} to observe \verb{\{open, pinned\}} state.}

\item{side}{Which side of the viewport the panel slides in from.}

\item{width}{A CSS length applied to the panel as
\code{--blockr-sidebar-panel-width}. The CSS bundle uses that variable for
the panel's \code{width}.}

\item{mode}{One of \code{"overlay"} (default) or \code{"push"}. In \code{"overlay"}
mode the panel hovers above the page and the rest of the layout is
untouched. In \code{"push"} mode the bundled CSS adds
\verb{padding-<side>: var(--blockr-sidebar-width)} on \verb{<body>} whenever
the panel is open, so the page content shifts aside instead of being
covered. The choice is independent of pin state.}

\item{ui}{A Shiny tag tree to render inside the panel body. Pre-rendered
via \code{\link[htmltools:renderTags]{htmltools::renderTags()}} so dependencies travel with the content.}

\item{title}{Optional title shown in the panel header. \code{NULL} clears it.}

\item{session}{The Shiny session. Defaults to the calling reactive
domain - same as \code{\link[shiny:showModal]{shiny::showModal()}}.}
}
\value{
\itemize{
\item \code{sidebar_ui()} returns an \link[htmltools:builder]{htmltools::tag} with \code{sidebar_dep()}
attached.
\item \code{sidebar_dep()} returns an \link[htmltools:htmlDependency]{htmltools::htmlDependency}.
\item \code{show_sidebar()} and \code{hide_sidebar()} are called for their side
effect (a custom message to the client) and return \code{invisible(NULL)}.
\item \code{sidebar_state()} returns a list \code{list(open, pinned)} of logicals,
read from the root session's input. Useful when an action handler
wants to decide whether to chain (re-render the form) or close the
panel after a successful confirm: pinned panels should stay open
per the user's opt-in, unpinned panels close.
\item \code{keep_or_hide_sidebar()} is the convenience built on top of
\code{sidebar_state()} for the action-handler confirm flow: when the
panel is pinned it re-shows with the supplied (fresh) \code{ui} so the
user can chain another action; otherwise it hides the panel.
}
}
\description{
A Bootstrap-free slide-in panel mirroring \code{\link[shiny:showModal]{shiny::showModal()}} /
\code{\link[shiny:showModal]{shiny::removeModal()}} in spirit. \code{sidebar_ui()} mounts an empty panel
once per page; \code{show_sidebar()} and \code{hide_sidebar()} drive its content
from any \code{moduleServer()} body, exactly like a modal.
}
\examples{
if (interactive()) {
  library(shiny)
  ui <- fluidPage(
    actionButton("open", "Open sidebar"),
    sidebar_ui("panel", side = "right")
  )
  server <- function(input, output, session) {
    observeEvent(input$open, {
      show_sidebar(
        "panel",
        title = "Hello",
        ui = tagList(
          textInput("name", "Name"),
          actionButton("ok", "OK")
        )
      )
    })
    observeEvent(input$ok, hide_sidebar("panel"))
  }
  shinyApp(ui, server)
}

}
