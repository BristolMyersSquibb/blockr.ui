% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-preview.R
\name{build_html_table}
\alias{build_html_table}
\title{Build the HTML table preview for one page}
\usage{
build_html_table(
  dat,
  total_rows,
  sort_state = NULL,
  ns = NULL,
  page = 1L,
  page_size = 5L,
  table_label = NULL,
  sort_input = NULL,
  page_input = NULL
)
}
\arguments{
\item{dat}{Materialized data frame holding only the rows of the current
page.}

\item{total_rows}{Total number of rows in the full result (drives the
row-range text and pagination).}

\item{sort_state}{List with elements \code{col} and \code{dir} (\code{"asc"}, \code{"desc"},
\code{"na"} or \code{"none"}) or \code{NULL}.}

\item{ns}{Optional namespace function used for the legacy default input
ids when \code{sort_input} / \code{page_input} are not supplied.}

\item{page}{Current page (1-based).}

\item{page_size}{Rows per page.}

\item{table_label}{Optional table-level label shown in the footer.}

\item{sort_input, page_input}{Full (already namespaced) ids of the Shiny
inputs receiving sort and page events. Supply per-instance ids when a
session shows more than one preview; when \code{NULL} the legacy shared
names \code{blockr_table_sort} / \code{blockr_table_page} are used.}
}
\value{
A \code{\link[shiny:reexports]{shiny::tagList()}} with the table preview, carrying the
\code{\link[=table_preview_dep]{table_preview_dep()}} html dependency.
}
\description{
Pure presentation: renders an already-materialized page of data as the
blockr table preview (header with column names, labels and type tags,
pillar-formatted cells, NA / negative styling, footer with row range and
prev / next pagination). Data access (sorting, pagination, lazy tables)
is handled by \code{\link[=table_page]{table_page()}}.
}
