% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-preview-engine.R
\name{table_page}
\alias{table_page}
\title{Fetch one page of a tabular result}
\usage{
table_page(
  result,
  sort_state = NULL,
  page = 1L,
  page_size = 5L,
  cache = new.env(parent = emptyenv())
)
}
\arguments{
\item{result}{A data frame or lazy table (\code{tbl_lazy}).}

\item{sort_state}{List with elements \code{col} and \code{dir} (\code{"asc"}, \code{"desc"},
\code{"na"} or \code{"none"}) or \code{NULL} for unsorted.}

\item{page}{Requested page (1-based; clamped to the valid range).}

\item{page_size}{Rows per page.}

\item{cache}{Environment used to memoize the sort index (local) and the
row count (lazy) across interactions on the same \code{result}. Callers
should keep one cache per result; the default creates a fresh one.}
}
\value{
A list with \code{dat} (materialized page as a data frame),
\code{total_rows} and \code{page} (after clamping).
}
\description{
Resolves sorting and pagination for the table preview and returns a single
materialized page. In-memory data frames are sorted via a cached order
index on the sort column only (O(page) on page navigation); lazy dbplyr
tables are counted and paged in the database via a \code{ROW_NUMBER()} window
query, so the full table is never collected.
}
\details{
NA placement matches local \code{\link[dplyr:arrange]{dplyr::arrange()}} semantics on both backends
(\code{asc} / \code{desc} put NA last, \code{na} puts NA first): SQL \verb{ORDER BY} would put
NULLs first, so the remote sort keys are normalized with an explicit
\code{is.na()} key.
}
