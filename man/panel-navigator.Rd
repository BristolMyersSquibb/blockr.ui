% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-navigator.R
\name{panel-navigator}
\alias{panel-navigator}
\alias{panel_navigator_ui}
\alias{panel_navigator_server}
\title{Panel navigator sidebar}
\usage{
panel_navigator_ui(id, model = list())

panel_navigator_server(id, board = NULL, update = NULL)
}
\arguments{
\item{id}{Module id.}

\item{model}{A list of groups. Each group is
\code{list(name=, color=, kind=, entries=)} where \code{kind} is one of
\code{"stack"}, \code{"ungrouped"}, \code{"extensions"}, and each entry is
\code{list(id=, type=, title=, subtitle=, icon=, package=, color=, on_view=, tags=)}.}

\item{board}{Reactive returning the current board object. When supplied
with \code{update}, the navigator applies the pure-blockr.core events itself
(stack reassign / reorder, block / stack rename) — the same division of
labour as \code{stack_menu_server()}. May be \code{NULL} for the legacy
"surface every event to the caller" behaviour.}

\item{update}{Board-update callback for the core events above.}
}
\value{
\code{panel_navigator_ui()} returns a \link[shiny:reexports]{shiny::tag} with the navigator
dependency attached. \code{panel_navigator_server()} returns a
\link[shiny:reactive]{shiny::reactive} firing \code{list(id=, type=, to=)} on each eye toggle,
where \code{to} is \code{"add"} or \code{"remove"}.
}
\description{
A sidebar that lists every panel on a board, grouped by stack, with a
per-entry eye toggle reflecting whether the panel is on the current view
and tags for the other views it appears on. It mirrors the visual language
of the \link{block-browser} (same card chrome and CSS), but lists \emph{existing}
panels rather than block constructors: clicking an entry's eye toggles its
membership in the current view.
}
\details{
Unlike the block browser, the navigator's content depends on live board /
dock state, so its body is rendered per open by the consumer (blockr.dock)
and passed to \code{\link[=show_sidebar]{show_sidebar()}}. The consumer assembles the \code{model}.
}
